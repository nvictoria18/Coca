import { TypesOfScreen } from "@/types/Screen";

const textScreenType: Record<TypesOfScreen, string> = {
  laptop: 'Join our team, The one with the master touch',
  mobile: 'Join our team',
}

export default textScreenType;